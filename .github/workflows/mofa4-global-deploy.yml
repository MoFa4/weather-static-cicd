name: 🇦🇺 MoFa4 Weather SYDNEY DOWN UNDER

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Build App
        run: |
          npm ci
          npm run build

      - name: AWS Login Sydney
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to Sydney S3
        run: |
          aws s3 sync build/ s3://${{ secrets.AWS_S3_BUCKET }} --delete --region ap-southeast-2
          echo "🚀 MOFA4 SYDNEY LIVE: http://${{ secrets.AWS_S3_BUCKET }}.s3-website-ap-southeast-2.amazonaws.com"

      - name: Sydney Victory
        run: echo "MOFA JUST COLONIZED AUSTRALIA BABYYYY 🔥🇦🇺"